
event_inherited();

draw_set_valign(fa_top)
draw_set_halign(fa_left)

// The camera extension delegates to ACTION_IMAGE_CAPTURE and needs no runtime
// permissions, so there is no permission status to display here.
